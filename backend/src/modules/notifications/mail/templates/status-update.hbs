<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>GovConnect - Status Update</title>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .container { 
      border: 1px solid #ddd; 
      border-radius: 5px; 
      padding: 20px; 
      background-color: #f9f9f9;
    }
    .status-box {
      font-size: 18px;
      text-align: center;
      padding: 15px;
      margin: 20px 0;
      color: white;
      border-radius: 5px;
    }
    .status-approved {
      background: #28a745;
    }
    .status-pending {
      background: #007bff;
    }
    .status-action-required {
      background: #dc3545;
    }
    .status-in-progress {
      background: #6c757d;
    }
    .service-details {
      background: #f0f0f0;
      border-left: 4px solid #007bff;
      padding: 15px;
      margin: 15px 0;
    }
    .detail-row {
      display: flex;
      margin-bottom: 8px;
    }
    .detail-label {
      font-weight: bold;
      width: 120px;
    }
    .message-box {
      background: #e8f4fc;
      border: 1px solid #b8daff;
      border-radius: 5px;
      padding: 15px;
      margin: 20px 0;
    }
    .action-button {
      display: inline-block;
      padding: 10px 20px;
      margin: 15px 0;
      background: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      text-align: center;
    }
    .additional-documents {
      background: #fff3cd;
      border-left: 4px solid #ffc107;
      padding: 15px;
      margin: 15px 0;
    }
    .document-item {
      margin-bottom: 10px;
      padding-left: 25px;
      position: relative;
    }
    .document-item:before {
      content: "!";
      position: absolute;
      left: 0;
      color: #dc3545;
      font-weight: bold;
      width: 18px;
      height: 18px;
      line-height: 18px;
      text-align: center;
      background: #fff3cd;
      border-radius: 50%;
      border: 1px solid #dc3545;
    }
    .deadline {
      color: #dc3545;
      font-weight: bold;
    }
    .footer {
      font-size: 12px;
      color: #777;
      margin-top: 30px;
      text-align: center;
    }
    .header {
      text-align: center;
      color: #007bff;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>GovConnect Status Update</h2>
    </div>
    
    <p>Hello {{name}},</p>
    <p>There has been an update regarding your service request with {{departmentName}}.</p>
    
    <div class="status-box {{statusClass}}">
      Status: {{status}}
    </div>
    
    <div class="service-details">
      <div class="detail-row">
        <div class="detail-label">Service:</div>
        <div>{{serviceName}}</div>
      </div>
      <div class="detail-row">
        <div class="detail-label">Reference No:</div>
        <div>{{referenceNumber}}</div>
      </div>
      <div class="detail-row">
        <div class="detail-label">Updated On:</div>
        <div>{{updateDate}}</div>
      </div>
      <div class="detail-row">
        <div class="detail-label">Updated By:</div>
        <div>{{officerName}}</div>
      </div>
    </div>

    <div class="message-box">
      <h3>Message from {{departmentName}}:</h3>
      <p>{{message}}</p>
    </div>

    {{#if additionalDocumentsRequired}}
      <div class="additional-documents">
        <h3>Additional Documents Required:</h3>
        {{#each additionalDocuments}}
          <div class="document-item">{{this}}</div>
        {{/each}}
        <p>Please submit these documents by <span class="deadline">{{deadline}}</span> to avoid delays.</p>
      </div>
      <a href="{{portalUrl}}" class="action-button">Submit Documents</a>
    {{/if}}

    {{#if nextAppointmentRequired}}
      <p>A follow-up appointment is required. Please schedule your appointment through your GovConnect portal.</p>
      <a href="{{appointmentUrl}}" class="action-button">Schedule Appointment</a>
    {{/if}}

    {{#if isCompleted}}
      <p>Your service request has been completed. No further action is required from your end.</p>
      <p>You can download your documents from your GovConnect portal.</p>
      <a href="{{downloadUrl}}" class="action-button">View Documents</a>
    {{/if}}
    
    <p>If you have any questions, please log in to your GovConnect account to send a message or contact our support team.</p>
    
    <div class="footer">
      <p>This is an automated message, please do not reply to this email.</p>
      <p>&copy; GovConnect - Your gateway to government services</p>
    </div>
  </div>
</body>
</html>
