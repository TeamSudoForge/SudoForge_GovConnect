FROM node:20-alpine

WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Use npm install instead of npm ci for development
RUN npm install

# Copy the rest of the application
COPY . .

# Expose API port
EXPOSE 3000

# Use nodemon for development to watch for changes
CMD ["npm", "run", "start:dev"]